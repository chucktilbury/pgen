
#include <iostream>
#include "scanner.h"
#include "ast.h"

AstGrammar* ParseGrammar(ParserState* state);
AstRule* ParseRule(ParserState* state);
AstNonTerminal* ParseNonTerminal(ParserState* state);
AstTerminal* ParseTerminal(ParserState* state);
AstExpression* ParseExpression(ParserState* state);
AstSelect* ParseSelect(ParserState* state);
AstSequence* ParseSequence(ParserState* state);
AstZeroOrOne* ParseZeroOrOne(ParserState* state);
AstZeroOrMore* ParseZeroOrMore(ParserState* state);
AstOneOrMore* ParseOneOrMore(ParserState* state);
AstNot* ParseNot(ParserState* state);

AstGrammar* ParseGrammar(ParserState* state) {

    ENTER;
    RETURN(nullptr);
}

AstRule* ParseRule(ParserState* state) {

    ENTER;
    RETURN(nullptr);
}

AstNonTerminal* ParseNonTerminal(ParserState* state) {

    ENTER;
    RETURN(nullptr);
}

AstTerminal* ParseTerminal(ParserState* state) {

    ENTER;
    RETURN(nullptr);
}

AstExpression* ParseExpression(ParserState* state) {

    ENTER;
    RETURN(nullptr);
}

AstSelect* ParseSelect(ParserState* state) {

    ENTER;
    RETURN(nullptr);
}

AstSequence* ParseSequence(ParserState* state) {

    ENTER;
    RETURN(nullptr);
}

AstZeroOrOne* ParseZeroOrOne(ParserState* state) {

    ENTER;
    RETURN(nullptr);
}

AstZeroOrMore* ParseZeroOrMore(ParserState* state) {

    ENTER;
    RETURN(nullptr);
}

AstOneOrMore* ParseOneOrMore(ParserState* state) {

    ENTER;
    RETURN(nullptr);
}

AstNot* ParseNot(ParserState* state) {

    ENTER;
    RETURN(nullptr);
}

